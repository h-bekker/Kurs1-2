#include <iostream>
#include <vector>
using namespace std;

int dlina (vector <int> v) {
	

int main() {
	vector <int> v;
	int n;
	cin >> n;
	int k;
	for (int i=0; i<n; i++)
	{
		cin >> k;
		v.push_back(k);
	}
	int ans;
	ans=dlina(v);
	cout << ans << endl;
	}
